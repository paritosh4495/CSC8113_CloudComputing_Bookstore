winget install velero


@"
[default]
aws_access_key_id=PASTE_YOUR_KEY_HERE
aws_secret_access_key=PASTE_YOUR_SECRET_HERE
"@ | Out-File -FilePath "./credentials-velero" -Encoding ascii


velero install `
  --provider aws `
  --plugins velero/velero-plugin-for-aws:v1.9.0 `
  --bucket bookstore-velero-backups-csc8113 `
  --secret-file "./credentials-velero" `
  --backup-location-config region=eu-west-2 `
  --use-volume-snapshots=false `
  --use-node-agent


fix any isseues

velero backup-location get
-> This should say available



kubectl apply -f deployment/k8s/postgres.yml


velero backup create postgres-backup-manual --include-namespaces default --wait


velero backup describe postgres-backup-manual


velero schedule create daily-postgres-backup `
  --schedule="0 2 * * *" `
  --include-namespaces default `
  --ttl 72h0m0s


velero schedule get


# Delete the StatefulSet (kills the postgres pod)
kubectl delete statefulset postgres


# Delete the PVC (wipes all the data)
kubectl delete pvc postgres-storage-postgres-0


kubectl get statefulset
kubectl get pvc
kubectl get pods | Select-String "postgres"


velero backup get


velero restore create --from-backup postgres-backup-manual --wait

























// Steps :

1. Show the schedules
-> velero schedule get

2. Show the backups
-> velero backup get
-> Show the backup in AWS S3 bucket as well

// Start the Disaster
3. Delete the StatefulSet (which inturn deletes the pod automatically) and PVC
-> kubectl get statefulset
-> kubectl get pvc
-> kubectl delete statefulset postgres
-> kubectl delete pvc postgres-storage-postgres-0

// Show the data is vanished
-> FRONT END : HIT THE ENDPOINT

// Start the Recover process
-> velero backup get
-> velero restore create --from-backup <NameOfTheBackup> --wait

// Show the restore is successful
-> kubectl get pods -w
-> kubectl get statefulset
-> kubectl get pvc

// Show the data is back
-> FRONT END : HIT THE ENDPOINT



















